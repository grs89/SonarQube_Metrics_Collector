apiVersion: 1

providers:
  - name: 'SonarQube Metrics'
    orgId: 1
    folder: 'SonarQube'
    folderUid: 'sonarqube'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

